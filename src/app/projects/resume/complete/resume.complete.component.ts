import { Component, Input, OnInit  } from '@angular/core';

@Component({
  selector: 'app-projects-resume-complete',
  templateUrl: './resume.complete.component.html',
})
export class ProjectsResumeCompleteComponent implements OnInit {

    constructor( ) { }

    ngOnInit(): void { }
}
